# ── roles/app/templates/env.j2 ──────────────────────────────
# Jinja2 template — Ansible điền biến vào {{ }}
# Giống như f-string trong Python

DB_PASSWORD={{ db_password }}
GRAFANA_PASSWORD={{ grafana_password }}

# Ansible tự điền các giá trị này từ vars hoặc vault
